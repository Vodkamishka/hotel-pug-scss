mixin demo (options)
    - const {websiteTitles, uikitTitles} = options
    .demo
        .demo__see See demo
        .demo__title Website Pages
        for el in websiteTitles
            a(href=`/${el.link}.html` target="_blank" class="demo__a")= el.title
        .demo__title Ui Kit 
        for el in uikitTitles
            a(href=`/${el.link}.html` target="_blank" class="demo__a")= el.title   