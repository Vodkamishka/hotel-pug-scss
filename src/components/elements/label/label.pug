mixin label (props)
    - const {classLabel, leftLabel, rightLabel} = props
    .label(class=`label_${classLabel}`)
        label(for="forLab").label__text= leftLabel
        label(for="forLab").label__text= rightLabel