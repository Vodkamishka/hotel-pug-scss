include ../button-login/button-login
include ../button-register/button-register

mixin buttons-login-register(props)
    - const {log, register} = props
    .buttons-login-register
        .buttons-login-register__login
            +button-login(log)
        .buttons-login-register__register
            +button-register(register)
