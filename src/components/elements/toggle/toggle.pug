include ../toggle-one/toggle-one

mixin toggle (show)
    .toggle
        if show
            .toggle__title Toggle
        .toggle__buttons
            .toggle__buttons_top
                +toggle-one(true)
            .toggle__buttons_bottom
                +toggle-one(false)
        


    