mixin navbar (list)
    .navbar
        ul
            for el in list
                if el.image
                    li 
                        span= el.item
                        .navbar__image
                else 
                    li 
                        span= el.item

